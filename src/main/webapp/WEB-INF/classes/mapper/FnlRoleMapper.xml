<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smm.payCenter.dao.RoleDao">

	<resultMap id="BaseResultMap" type="com.smm.payCenter.domain.Role" >
	    <!--
	      WARNING - @mbggenerated
	      This element is automatically generated by MyBatis Generator, do not modify.
	      This element was generated on Tue Nov 17 11:08:13 CST 2015.
	    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="role_name" jdbcType="VARCHAR" property="roleName" />
    <result column="description" jdbcType="VARCHAR" property="description" />
    <result column="create_time" jdbcType="VARCHAR" property="createTime" />
    <result column="role_status" jdbcType="INTEGER" property="roleStatus" />
  	</resultMap>
    <select id="getRoleById" resultType="com.smm.payCenter.domain.Role" parameterType="Integer">
    
    	select 
		 id,
		
		 role_name,
		
		  description
    	from  fnl_role where  id=#{roleId}

    </select>
    <!-- 新增角色 -->
	<insert id="addRole" parameterType="com.smm.payCenter.domain.Role">
	  	
	  	INSERT into fnl_role
	  	
	  	(role_name, description,create_time, role_status)
	  	
	    values (#{roleName,jdbcType=VARCHAR}, #{description,jdbcType=VARCHAR},#{createTime,jdbcType=VARCHAR}, #{roleStatus,jdbcType=INTEGER})
	   
		<selectKey keyProperty="id" resultType="int" order="AFTER">
			select LAST_INSERT_ID() 
		</selectKey>
  	</insert>
  	<!-- 符合查询条件角色数目 -->
  	<select id="queryRoleCount" parameterType="java.util.HashMap" resultType="Integer">
  		
  		select count(1) from 
  		(
  			SELECT * FROM fnl_role 
		
			where 1 = 1
			
			<if test=" roleName != null and roleName != ''">
	  			and role_name like  CONCAT('%',#{roleName},'%')
	  		</if>
			
			ORDER BY id DESC
		) temp
  	
  	</select>
    <!-- 符合查询条件角色 -->
    <select id="queryRoleList" parameterType="java.util.HashMap" resultMap="BaseResultMap">
    	
    	SELECT * FROM fnl_role 
		
		where 1 = 1
		
		<if test=" roleName != null and roleName != ''">
  			and role_name like  CONCAT('%',#{roleName},'%')
  		</if>
		
		ORDER BY id DESC
		
		<!-- 分页 -->
		<if test="startNum!=null and endNum !=null ">
			limit ${startNum},${endNum}
		</if>
		
    </select>
    <!-- 删除权限 -->
    <delete id="deleteAuthorization">
    	DELETE FROM fnl_role_relation where role_id = #{roleId}
    </delete>
    
    <select id="queryRoleById" parameterType="java.util.HashMap" resultMap="BaseResultMap"> 
    
    	SELECT * FROM fnl_role 
    	
    	where id = #{roleId}
		
    
    </select>

</mapper>